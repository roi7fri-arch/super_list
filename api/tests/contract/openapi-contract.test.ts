/**
 * Phase 2 contract harness placeholder.
 * Real test runner assertions are wired in US1/US2 contract tasks.
 */
export const openApiContractChecklist = [
  '/households/{householdId}/list',
  '/households/{householdId}/items:addByVoice',
  '/households/{householdId}/items/{itemId}:remove',
];
